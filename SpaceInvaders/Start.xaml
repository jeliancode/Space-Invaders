<Page
    x:Class="SpaceInvaders.InicioSesion"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SpaceInvaders"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    NavigationCacheMode="Required"
    mc:Ignorable="d"
    Background="Black">

  <Page.Resources>
    <Style x:Key="LegoTextStyle" TargetType="TextBlock">
      <Setter Property="FontSize" Value="28" />
      <Setter Property="Foreground" Value="#FFFFFFFF" />
    </Style>
  </Page.Resources>

  <Grid Margin="0,70,0,0">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <TextBlock
      Text="SPACE INVADERS"
      FontSize="54"
      Margin="30"
      HorizontalAlignment="Center"
      VerticalAlignment="Top"
      Foreground="#7A0606">
    </TextBlock>

    <StackPanel Grid.Row="1" Orientation="Vertical" Margin="150" HorizontalAlignment="Center">
      <Button Click="Play_Click" Background="Transparent" BorderThickness="0" HorizontalAlignment="Center" Margin="0,40,0,25" >
        <TextBlock Style="{StaticResource LegoTextStyle}">PLAY</TextBlock>
      </Button>
    </StackPanel>
  </Grid>
</Page>
